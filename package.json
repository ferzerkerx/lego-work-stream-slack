{
  "name": "lego-work-stream-slack",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "install": "(cd backend && yarn install) && (cd frontend && yarn install)",
    "build": "(cd backend && yarn compile) && (cd frontend && yarn build) && copyfiles -f frontend/dist/* backend/dist/public",
    "test": "(cd backend && yarn test) && (cd frontend && yarn test)",
    "start": "(cd backend && yarn start)",
    "prettier": "(cd backend && yarn prettier) && (cd frontend && yarn prettier)"
  },
  "dependencies": {},
  "author": "ferzerkerx",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.1.0"
  },
   "resolutions": {
    "**/base64url": "^3.0.0"
  },
  "engines": {
    "node": "^8.9.0",
    "yarn": "^1.3.2"
  }
}
